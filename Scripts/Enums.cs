namespace CoasterForge {
    public enum DurationType {
        Time,
        Distance,
    }

    public enum SectionType {
        Geometric,
        Force,
    }

    public enum InterpolationType {
        Constant,
        Linear,
        Ease,
    }
}
